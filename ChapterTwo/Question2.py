#(What’s wrong with this code?) The following code should read an
#integer into the variable rating:
#rating = input('Enter an integer rating
#between 1 and 10')


rating = int(input('Enter an integer rating\
between 1 and 10'))